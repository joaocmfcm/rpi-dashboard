'use strict';

/**
 * @description
 * Application core configurations
 * @observations
 * 
 */

angular.module('dashboardApp')
	.run(runBlock)
	.config(configBlock);

runBlock.$inject = [];
configBlock.$inject = [];

function runBlock(){

}

function configBlock(){

}